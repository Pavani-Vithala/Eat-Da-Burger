<h1>Eat Da Burger!</h1>
<br>
<div id ="image">
<img src = "/assets/img/burger.png" alt="Burger" class="center" id ="BURG">
</div>
<br><br>

<h3>Burgers that are not devoured</h3>

<ul id = "noDevoured">
    {{#each burger}}
    {{#unless devoured}}
    {{>burgers/burger-block devoured=false}}
    {{/unless}}
    {{/each}}
</ul>

<h3> Burgers that are Devoured</h3>


<ul id = "Devoured">
    {{#each burger}}
    {{#if devoured}}
    {{>burgers/burger-block devoured=true}}
    {{/if}}
    {{/each}}
</ul>

<h3>Add a burger</h3>

<form class="create-form">

    <div class="form-group">
        <label for="ba">Burger Name:</label>
        <input type="text" id="ba" name="name">
        <p>Type a burger Name and click submit</p>
    </div>

    <button type="submit">Add Burger</button>
</form>